import psutil

def listar_procesos():
    for proceso in psutil, process_iter([`pid`, `Name`, `status`]):
 try: 
print(f*PID:
 {proceso.info[`pid`]}, Name:
                  {proceso.info[`name`]}, Estado:
                  {proceso.info[`status`]}")
                  except
                  (psutil.NoSuchProcess,
                   psutil.AccessDenied):
                   continue     
                   listar_procesos()
                   )


